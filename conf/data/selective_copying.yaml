_target_: src.data.selectivecopying_datamodule.SelectiveCopyingDataModule
l_noise: 100
l_memorize: 10
n_tokens: 16
variable: true
variable_length: false
one_hot: false
reverse: false
batch_size: 32
num_workers: 4
train_size: 10000 
val_size: 1000
test_size: 1000
seed: ${seed}

causal: false  # Selective copying doesn't need causal attention

# dataset properties
# sequence_length: Total input sequence length
# input_size: Input vocabulary size
# output_size: Output vocabulary size
# target_length: Target sequence length
